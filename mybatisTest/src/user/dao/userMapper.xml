<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userSQL">
	<insert id="write" parameterType="user.bean.UserDTO">
		insert into usertable values(#{name},#{id},#{pwd})
	</insert>
	<delete id="delete" parameterType="String">
		delete from usertable where id= #{id}
	</delete>
	<select id="getList" resultType="user.bean.UserDTO">
		select * from usertable
	</select>
	<select id="search" parameterType="String" resultType="user.bean.UserDTO">
		select * from usertable where id= #{id}
	</select>
	<update id="update" parameterType="user.bean.UserDTO">
		update usertable set name=#{name}, pwd=#{pwd} where id=#{id}
	</update>
</mapper>
